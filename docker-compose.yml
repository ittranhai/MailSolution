# version: '3.4'

services:
  services.Api:
    image: ${DOCKER_REGISTRY-}mail-api
    build:
      context: .
      dockerfile: Api/Dockerfile
  services.App:
    image: ${DOCKER_REGISTRY-}app
    build:
      context: .
      dockerfile: App/Dockerfile